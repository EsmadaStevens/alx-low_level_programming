#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @r: the value to be checked
 * Return: absolute value
 */
int _abs(int r)
{
	if (r >= 0)
	{
		return (r);
	}
	return (-r);
}
